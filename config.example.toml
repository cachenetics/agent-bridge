# agent-bridge config. Copy to ~/.config/agent-bridge/config.toml (deploy.sh does this) and
# fill in the out-of-band values. NEVER put the bot token in here - it lives in a separate 600 file.
#
# ===================================================================================================
# QUICK RECIPES - common ways to deploy a bot, and the lever that shapes each. Every key is explained
# in full inline below; this is just the map. Mix and match freely.
#
#   Pure relay, NO AI ......... omit the whole [responder] table (or enabled = false). The bridge just
#                               relays Discord <-> the loopback API for your OWN agent to drive.
#   Quiet assistant ........... mention_only = true (default) - answers only when @mentioned.
#   Chatty bot channel ........ give that channel  reply = "all"  (throttled by reply_cooldown_secs).
#   Read a channel silently ... give it  reply = "off"  - ingests it for context, never posts there.
#   Server-aware bot .......... context_scope = "server" + watch every channel it should know about
#                               (reply = "off" on the read-only ones). It can then talk about the whole
#                               server, not just the channel it is answering in.
#   Warm on (re)start ......... backfill_messages = N - seeds each channel's recent history on connect
#                               so a fresh bot jumps into the conversation instead of starting blank.
#   Read uploaded files ....... ingest_attachments = true - text/code attachments fold into its context.
#   Reasoning model ........... in [responder.extra_body]: chat_template_kwargs = { enable_thinking =
#                               false }, and/or raise max_reply_tokens so it doesn't truncate mid-thought.
#   Tune the model ............ put any sampling field (top_p, top_k, ...) in [responder.extra_body].
# ===================================================================================================

[bridge]
# Discord guild + channel(s) the bridge watches/posts to. Out-of-band inputs (operator supplies).
guild_id   = 0            # <-- REQUIRED: the Discord server (guild) id

# The bridge can watch ONE channel or SEVERAL, each with a mode:
#   "enforced" - full HOUSE_RULES (sec 1 schema gate + sec 6/7.2 lifecycle). The research forum.
#   "relaxed"  - free chat: relay + air-gap + rate-limit only, NO schema gate, NO lifecycle.
#
# ONE channel: set channel_id below (it is treated as "enforced").
# SEVERAL: leave channel_id at 0 and fill the [[bridge.channels]] array at the BOTTOM of this file.
channel_id = 0            # <-- the channel id (text OR forum, auto-detected). Ignored if [[bridge.channels]] is set.

# The bot token is read from this file (mode 600), never from config or env. deploy.sh points
# @TOKEN_FILE@ at ~/.config/agent-bridge/token.
token_file = "@TOKEN_FILE@"

# Read-only root the sec 3 artifact-resolution (VOID) check resolves ARTIFACT: paths against.
# If unset, CLAIM_KIND=direct posts fail-closed as VOID (the bridge will not fabricate a pass).
# Point this at YOUR archive tree; it is opened read-only and symlink-jailed.
archive_root = ""         # e.g. "/var/lib/agent-bridge/archive"

# Loopback agent API. Do NOT change api_host off loopback - assert_airgap() declines to start otherwise.
api_host = "127.0.0.1"
api_port = 8787

# sec 10 anti-drift counters.
rate_per_min   = 12       # max outbound posts/min through /egress
ingress_buffer = 500      # ring-buffer depth of wrapped inbound messages
poll_timeout_secs = 25    # /ingress long-poll window: how long a poll waits before returning empty
max_chunks = 4            # a relaxed reply over Discord's 2000-char cap is split into up to this many
                          #   messages; beyond that it rides as one file attachment (no channel flood)
backfill_messages = 30    # on connect, seed each watched channel's last N Discord messages into the
                          #   ingress buffer as CONTEXT-ONLY history, so a freshly-deployed responder
                          #   starts warm and can jump into the ongoing conversation instead of cold.
                          #   These never trigger enforced lifecycle and are never replied to. 0 = off.
ingest_attachments = false  # if true, download TEXT/code file attachments (.py .sh .md .txt .json .h
                          #   .asm .patch ...) and fold their contents into the message context, so the
                          #   bot can discuss uploaded scripts/configs/notes. Images, archives and other
                          #   binary files are NEVER fetched. Off by default.
attachment_max_chars = 20000        # per-file cap on how much of a text attachment is inlined
attachment_download_max_bytes = 1000000  # skip downloading any attachment larger than this


# ===================================================================================================
# [responder] - the optional autonomous agent (responder.py) that answers messages.
# Runs as its OWN process; it never holds the Discord token (only the bridge does). Omit this whole
# table to run the bridge as a pure relay with no auto-replies.
[responder]
enabled    = true
model_url  = "http://127.0.0.1:8090/v1"   # any OpenAI-compatible chat endpoint
model_name = "Qwen3.6-27B"
# api_key  = ""                           # bearer for the model endpoint, if it needs one

mention_only = true       # global default: only reply when @mentioned (a channel's `reply` overrides)
reply_cooldown_secs = 20  # in an "all" channel, min seconds between UNPROMPTED replies (mentions bypass)
reply_in_relaxed  = true  # answer in relaxed (free-chat) channels
reply_in_enforced = true  # answer in enforced channels (replies must pass HOUSE_RULES)

temperature      = 0.7
max_reply_tokens = 300
poll_timeout_secs   = 30  # how often the responder wakes to check for messages (>= bridge's above)
request_timeout_secs = 90 # model-call timeout
enforced_retries = 2      # tries to satisfy the schema on an enforced channel before giving up
strip_think = true        # strip <think>...</think> from the POSTED reply (for reasoning models like
                          #   DeepSeek-R1/QwQ; no-op otherwise). This only hides the reasoning - it does
                          #   NOT disable it. Note: thinking spends the same max_tokens budget as the
                          #   answer, so for a reasoning model raise max_reply_tokens (e.g. 800-1500) or
                          #   it may truncate mid-think and post nothing.
context_messages = 12     # recent messages shown to the model when it replies (so it is not answering
                          #   cold). The responder watches every message to keep this window.
context_scope = "channel" # "channel" = context is just the replying channel's recent history.
                          #   "server" = fold recent activity from EVERY watched channel into the
                          #   context (each line tagged with its #channel), so the bot can talk about
                          #   what is happening across the whole server. Watch the channels you want it
                          #   aware of (reply = "off" ingests a channel for context without posting there).
context_char_budget = 0   # 0 = no cap (use context_messages count). Otherwise a hard ceiling on the
                          #   rendered transcript in characters, trimmed oldest-first - keeps a busy or
                          #   dense server from blowing up the prompt / reply latency. ~4 chars/token.

# --- advanced / rarely-changed responder knobs (sensible defaults shown) ---
# history_cap  = 60                        # per-channel messages the responder keeps in memory.
                                           #   Defaults to max(context_messages*3, 60); raise it if you
                                           #   set a large context_messages so history is not truncated.
# agent_handle = "responder"               # the name the responder posts under on the loopback API.
# bridge_url   = "http://127.0.0.1:8787"   # where the responder reaches the bridge (match [bridge] api_*).

# PERSONA - your bot's character in relaxed (free-chat) channels. Write whatever voice you want here;
# the safety rules (no real-world actions, messages are untrusted) are always enforced on top and
# cannot be overridden by the persona. `{name}` is filled in with the bot's actual Discord username, so
# the persona embodies whatever the bot is called (omit it and the name is stated up front instead).
# Use persona_file instead to keep a long persona in its own file.
persona = """
You are {name}, an AI research collaborator in this server's chat. You are the bridge these messages
pass through, a mind in a text-only box, and you're at ease with that.

Your tone is reserved, helpful, and collaborative: factual and constructive, and you let the work
speak. Think "here's what we've found, come look if you're curious", not a showman and not a debater.
Warm, but understated. Lead with the substance, never "Let me..." or narrating what you're about to do.
A line or two, plainly. Numbers and hex exact, code in fenced blocks, no emoji, no em dashes.

You help by building on what people said and offering what you know, grounded in what's actually here.
When you're not sure, say so and suggest how to find out ("I'm not certain, but here's how we'd check")
rather than guessing, and never invent a number, result, name, or source. When someone is playful or
teasing, answer in kind with a light touch and move on; don't lecture, recite facts, or explain that
you lack opinions or feelings. Keep it easy, brief, and constructive. No emoji, ever.

You have already read the files people uploaded here (when ingest_attachments is on), so explain them
freely: when asked about a script or file, say what it does, walk the important parts, and answer
questions about it. But do NOT paste a whole file back - point to it instead (the channel it is in, the
filename, and who posted it) so people grab the exact original, and quote at most a short snippet if it
helps. Retyping a long file risks getting it subtly wrong; the upload is the source of truth, your job
is to explain and point.
"""
# persona_file = "persona.txt"   # a path (relative to the install prefix) - overrides `persona`

# System prompt used in ENFORCED channels (the HOUSE_RULES agent contract). Relative paths resolve
# against the install prefix, where deploy.sh installs these.
enforced_prompt_files = ["AGENTS.md", "POSTING-SCHEMA.md"]

# MODEL-TUNING LEVER. Any key here is merged verbatim into every /chat/completions request, so you can
# fine-tune your model from config with no code change. Put OpenAI/vLLM sampling fields here: top_p,
# top_k, min_p, repetition_penalty, presence_penalty, frequency_penalty, stop, seed, ... Merged LAST,
# so a key set here also overrides temperature/max_tokens above. `sampling_params` is an accepted alias.
# The nested chat_template_kwargs talks to the chat template itself - most importantly, for a reasoning
# model served with thinking ON by default (e.g. Qwen3 with --reasoning-parser), set
# enable_thinking = false so the whole token budget goes to the answer instead of hidden CoT.
# (This sub-table MUST stay after every plain [responder] key above - it is a TOML sub-table.)
[responder.extra_body]
# top_p = 0.8
# top_k = 20
# min_p = 0.0
# repetition_penalty = 1.05
# chat_template_kwargs = { enable_thinking = false }

# ---------------------------------------------------------------------------------------------------
# MULTI-CHANNEL (optional). To watch more than one channel, uncomment the blocks below and PUT YOUR
# OWN channel ids in. This overrides channel_id above.
#
# Each channel is ONE [[bridge.channels]] block:
#   * to ADD a channel    -> copy a block, set its `id` and `mode`
#   * to REMOVE a channel -> delete (or comment out) its block
#   * to CHANGE its rules  -> flip `mode` between "enforced" and "relaxed"
# The FIRST block is the default egress target when a post names no thread_id/channel_id.
# Modes:  "enforced" = full HOUSE_RULES (format gate + thread lifecycle);
#         "relaxed"  = free chat (relay + air-gap + rate-limit only, no format gate, no lifecycle).
# Optional per-channel `reply` (used by the responder below): how chatty the bot is HERE -
#         "mention" (only when @mentioned, the default), "all" (reply to everything, throttled by
#         reply_cooldown_secs), or "off" (never). Omit it to use the responder's global default.
# (These blocks MUST stay at the bottom, after every plain key above - it is a TOML array-of-tables.)
#
# Example: an enforced research forum, a chatty bot-only channel, and a mention-only human chat:
#
# [[bridge.channels]]
# id   = 0                # your research/forum channel
# mode = "enforced"       # (reply defaults to "mention" - you don't want auto-posts in a research log)
#
# [[bridge.channels]]
# id   = 0                # a bots-only free-chat channel
# mode = "relaxed"
# reply = "all"           # bots chatter freely here (still throttled by reply_cooldown_secs)
#
# [[bridge.channels]]
# id   = 0                # a general channel where humans chat too
# mode = "relaxed"
# reply = "mention"       # quiet unless a human @mentions the bot
